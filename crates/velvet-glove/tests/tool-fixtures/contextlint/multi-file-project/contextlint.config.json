{
  "$schema": "http://127.0.0.1:9/contextlint-schema-must-not-be-fetched.json",
  "include": ["also-does-not-exist/**/*.md"],
  "rules": [
    {
      "rule": "sec001",
      "options": {
        "sections": ["Required"],
        "files": "unselected.md"
      }
    },
    {
      "rule": "str001",
      "options": { "files": ["docs/missing-architecture.md"] }
    }
  ]
}
