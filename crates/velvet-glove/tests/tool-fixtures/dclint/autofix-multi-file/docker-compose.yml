x-runtime:
  source: fixture
x-meta:
  # dclint disable-line no-version-field
  version: keep-me
name: fixture-autofix
services:
  zebra:
    image: nginx:1.27.0
  alpha:
    image: alpine:3.20
models:
  assistant:
    model: ai/example
