source "https://rubygems.org"

# Keep the analyzer graph on the latest pre-native-extension RuboCop line.
# Base64 and Racc come from the checksum-pinned Ruby archive; every package
# Bundler adds is checksum-locked, platform-independent Ruby code.
gem "base64", "0.2.0"
gem "benchmark", "0.4.0"
gem "ostruct", "0.6.1"
gem "parser", "3.1.3.0"
gem "racc", "1.8.1"
gem "rubocop", "1.30.1"
gem "rubocop-ast", "1.18.0"
