/// contextlint — rule-based linter for structured Markdown documents.
///
/// DRAFT: shipped without a vetted fixture because contextlint requires a
/// project-local `contextlint.config.json` and is not commonly installed.
/// Verified against jdx/hk's reference; not exercised by Velvet Glove's
/// fixtures yet.

module velvet_glove.builtins.tools.Contextlint

import "../Config.pkl" as Config

spec: Config.ToolSpec = new Config.ToolSpec {
  id = "contextlint"
  displayName = "contextlint"
  executable = "contextlint"
  installHint = "install contextlint with `npm install -g contextlint`"
  workspaceIndicator = "contextlint.config.json"

  files = new Config.FileSelection {
    include = new Listing<String> {
      "*.md"; "**/*.md"
      "*.markdown"; "**/*.markdown"
    }
  }

  phases = new Mapping<String, Config.Phase> {
    ["verify"] = new Config.Phase {
      mode = "verify"
      argv = new Listing<String | Config.ArgToken> {
        new Config.ExtraArgs {}
        new Config.Files {}
      }
      exitCodes = new Config.ExitCodes {
        clean = new Listing<Int> { 0 }
        issues = new Listing<Int> { 1 }
      }
    }
  }
  phaseOrder = new Listing<String> { "verify" }
}
