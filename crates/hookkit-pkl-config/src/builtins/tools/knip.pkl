/// knip — find unused files, dependencies, and exports in JS/TS projects.

module velvet_glove.builtins.tools.Knip

import "../Config.pkl" as Config

spec: Config.ToolSpec = new Config.ToolSpec {
  id = "knip"
  displayName = "knip"
  executable = "knip"
  installHint = "install knip with `npm i -g knip` or add it to the project"
  workspaceIndicator = "package.json"

  files = new Config.FileSelection {
    include = new Listing<String> {
      "*.js"; "**/*.js"
      "*.mjs"; "**/*.mjs"
      "*.cjs"; "**/*.cjs"
      "*.jsx"; "**/*.jsx"
      "*.ts"; "**/*.ts"
      "*.mts"; "**/*.mts"
      "*.cts"; "**/*.cts"
      "*.tsx"; "**/*.tsx"
    }
  }

  phases = new Mapping<String, Config.Phase> {
    ["fix"] = new Config.Phase {
      mode = "fix"
      argv = new Listing<String | Config.ArgToken> {
        "--fix"
        "--no-progress"
        "--directory"
        new Config.Workspace {}
        new Config.ExtraArgs {}
      }
      exitCodes = new Config.ExitCodes {
        clean = new Listing<Int> { 0 }
        issues = new Listing<Int> { 1 }
      }
      writes = "workspace"
    }
    ["verify"] = new Config.Phase {
      mode = "verify"
      argv = new Listing<String | Config.ArgToken> {
        "--no-progress"
        "--directory"
        new Config.Workspace {}
        new Config.ExtraArgs {}
      }
      exitCodes = new Config.ExitCodes {
        clean = new Listing<Int> { 0 }
        issues = new Listing<Int> { 1 }
      }
    }
  }
  phaseOrder = new Listing<String> { "fix"; "verify" }
}
