/// lychee — fast async link checker for Markdown, HTML, and friends.
///
/// `hk` ships `lychee` as a `check`-only step over text files. The CLI is
/// straightforward — pass the input paths and inspect the exit code. Empirically
/// (lychee 0.24), `0` is returned when every link is reachable and `2` is
/// returned when any link is broken; `1` is reserved for argument and config
/// errors. We map those onto our phase model accordingly.

module velvet_glove.builtins.tools.Lychee

import "../Config.pkl" as Config

spec: Config.ToolSpec = new Config.ToolSpec {
  id = "lychee"
  displayName = "lychee"
  executable = "lychee"
  installHint = "install lychee with `brew install lychee` or `cargo install lychee`"

  files = new Config.FileSelection {
    include = new Listing<String> {
      "*.md"; "**/*.md"
      "*.markdown"; "**/*.markdown"
      "*.mdx"; "**/*.mdx"
      "*.html"; "**/*.html"
      "*.htm"; "**/*.htm"
      "*.rst"; "**/*.rst"
      "*.txt"; "**/*.txt"
    }
  }

  phases = new Mapping<String, Config.Phase> {
    ["verify"] = new Config.Phase {
      mode = "verify"
      argv = new Listing<String | Config.ArgToken> {
        "--no-progress"
        new Config.ExtraArgs {}
        "--"
        new Config.Files {}
      }
      exitCodes = new Config.ExitCodes {
        clean = new Listing<Int> { 0 }
        issues = new Listing<Int> { 2 }
        failure = new Listing<Int> { 1 }
      }
    }
  }
  phaseOrder = new Listing<String> { "verify" }
}
