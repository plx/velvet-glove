/// tsc — TypeScript compiler used as a type checker.

module velvet_glove.builtins.tools.Tsc

import "../Config.pkl" as Config

spec: Config.ToolSpec = new Config.ToolSpec {
  id = "tsc"
  displayName = "tsc"
  executable = "tsc"
  installHint = "install TypeScript with `npm i -g typescript` or add it to the project"
  workspaceIndicator = "tsconfig.json"

  files = new Config.FileSelection {
    include = new Listing<String> {
      "*.ts"; "**/*.ts"
      "*.tsx"; "**/*.tsx"
      "*.mts"; "**/*.mts"
      "*.cts"; "**/*.cts"
    }
  }

  phases = new Mapping<String, Config.Phase> {
    ["verify"] = new Config.Phase {
      mode = "verify"
      argv = new Listing<String | Config.ArgToken> {
        "--noEmit"
        "-p"
        new Config.WorkspaceIndicator {}
        new Config.ExtraArgs {}
      }
      exitCodes = new Config.ExitCodes {
        clean = new Listing<Int> { 0 }
        issues = new Listing<Int> { 1; 2 }
      }
    }
  }
  phaseOrder = new Listing<String> { "verify" }
}
