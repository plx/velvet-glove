/// stylua — opinionated Lua formatter (Rust-based).

module velvet_glove.builtins.tools.Stylua

import "../Config.pkl" as Config

spec: Config.ToolSpec = new Config.ToolSpec {
  id = "stylua"
  displayName = "StyLua"
  executable = "stylua"
  installHint = "install stylua with `brew install stylua` or `cargo install stylua`"

  files = new Config.FileSelection {
    include = new Listing<String> { "*.lua"; "**/*.lua" }
  }

  phases = new Mapping<String, Config.Phase> {
    ["format"] = new Config.Phase {
      mode = "format"
      argv = new Listing<String | Config.ArgToken> {
        new Config.ExtraArgs {}
        new Config.Files {}
      }
      exitCodes = new Config.ExitCodes {
        clean = new Listing<Int> { 0 }
      }
      writes = "target-files"
    }
    ["verify"] = new Config.Phase {
      mode = "verify"
      argv = new Listing<String | Config.ArgToken> {
        "--check"
        new Config.ExtraArgs {}
        new Config.Files {}
      }
      exitCodes = new Config.ExitCodes {
        clean = new Listing<Int> { 0 }
        issues = new Listing<Int> { 1 }
      }
    }
  }
  phaseOrder = new Listing<String> { "format"; "verify" }
}
