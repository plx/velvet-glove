# Release packaging

This reference is a drafting skeleton for a follow-up session. Cover release
automation and checks for macOS and Linux on x86-64 and ARM64 before any
prebuilt binaries are added to the plugin. Treat Windows support as unverified
until the runner and built-in workflows have a dedicated compatibility lane.
