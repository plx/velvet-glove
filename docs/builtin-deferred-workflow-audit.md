# Built-in deferred workflow audit

<!-- markdownlint-disable MD013 -->

Generated from the embedded Pkl catalog. `explicit` rows use declared deferred workflows; `compatibility` rows are structurally translated from legacy phases by pairing each mutator with the final enabled read-only verifier. This inventory does not claim cross-version real-tool verification: command semantics remain version-dependent unless a limitation says otherwise, and the opt-in real-tool lane must use controlled tool versions. Batch and workspace findings are conservatively attributed to every candidate in the invocation when no diagnostic adapter identifies exact files.

| Built-in | Tool ID | Mode | Checks | Remedies | Check scope | Invocation | Precision / known limitation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| actionlint | actionlint | compatibility | verify: actionlint {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| alejandra | alejandra | compatibility | verify: alejandra --check {extra-args} {files} | format: alejandra {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| asciidoctor | asciidoctor | compatibility | verify: ruby -ropen3 -e <inline-script> -- {tool-executable} {extra-args} {files} | — | target-files | batch | The adapter rejects early-exit or diagnostic-suppression arguments, then a silent FATAL-threshold preflight maps CLI/configuration failures to exit 2 before the WARNING-threshold pass maps document diagnostics to exit 1; safe mode rejects ancestor includes, and batch inputs remain independent documents. |
| astro | astro | compatibility | verify: node --input-type=commonjs -e <inline-script> -- {tool-executable} check {extra-args} | — | workspace | workspace | The adapter disables telemetry, synchronization, watch mode, and timestamped output, rejects project-control and early-exit arguments, and accepts exit 0 or 1 only when a completed positive-file Result footer proves a workspace scan; incomplete runs and output beyond the explicit 16 MiB buffer map to exit 2. Validation intentionally fails on errors, not warnings or hints, and workspace findings are conservatively attributed to every selected candidate. |
| betterleaks | betterleaks | compatibility | verify: python -I -c <inline-script> {tool-executable} {extra-args} __VELVET_GLOVE_BETTERLEAKS_FILES__ {files} | — | target-files | batch | The isolated adapter preflights regular readable inputs, accepts only non-controlled long --name=value extra arguments, locks complete redaction, canonicalizes Betterleaks' fatal-log clock, reserves exit 10 for findings, and forwards cancellation with bounded child cleanup. Selected paths are scanned in one batch and conservatively share attribution; file targets require an explicit config, and the launch-time preflight cannot eliminate path replacement races. |
| biome | biome | compatibility | verify: python -I -c <inline-script> {tool-executable} verify {extra-args} __VELVET_GLOVE_BIOME_FILES__ {files} | fix: python -I -c <inline-script> {tool-executable} fix {extra-args} __VELVET_GLOVE_BIOME_FILES__ {files} | fix: target-files | batch | The isolated adapter preflights regular readable files, rejects extra arguments that alter selection, rule scope, mutation, reporting, or completion evidence, scrubs Biome/Node diagnostic overrides, and requires an exact-pinned JSON report proving every selected file was processed with no skips or truncated diagnostics. Status 1 maps to source issues only when every failing category is parse, format, lint, assist, or suppression syntax; configuration, incomplete, unexpected-category, signal, spawn, and output-over-16-MiB failures map to status 2. Safe fixes run in one batch and are followed by the authoritative check, so candidate attribution remains conservative while changed-file evidence stays exact; the experimental JSON schema is locked to the tested Biome patch release. |
| black | black | compatibility | verify: black --check {extra-args} {files} | format: black {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| brakeman | brakeman | compatibility | verify: brakeman --quiet --no-progress --no-summary -p {project-root} {extra-args} | — | workspace | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| bufFormat | buf-format | compatibility | verify: python -I -c <inline-script> {tool-executable} verify {extra-args} __VELVET_GLOVE_BUF_WORKSPACE__ {workspace} | format: python -I -c <inline-script> {tool-executable} write {extra-args} __VELVET_GLOVE_BUF_WORKSPACE__ {workspace} | format: workspace | workspace | The isolated adapter rejects every extra argument, requires a canonical v1/v2 workspace config, resolves the managed Buf executable before fixing PATH to the declared Apple diff prerequisite, disables symlink traversal, rejects hard-linked physical Proto files and Proto files beneath runner-hard-skipped directories, and runs a strict `buf config ls-modules` preflight that fails when any physical Proto file in the non-symlink workspace tree is outside the configured module/include scope or excluded from it. It bounds child output and accepts status 100 only with complete unified-diff blocks whose generated header mtimes it canonicalizes. A workspace invocation and workspace write scope cover every supported file, so findings are conservatively attributed to selected candidates while snapshot evidence records exact changes, including unselected matching files. Configurations that intentionally leave physical workspace Proto files outside Buf's module scope are unsupported; Buf format proves formatting cleanliness, not Proto syntax validity; late filesystem failures can partially mutate a workspace, and config/file checks cannot eliminate replacement races. |
| bufLint | buf-lint | compatibility | verify: buf lint {extra-args} {workspace} | — | workspace | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| buildifierFormat | buildifier-format | compatibility | verify: buildifier -mode=check {extra-args} {files} | format: buildifier {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| buildifierLint | buildifier-lint | compatibility | verify: buildifier -mode=check -lint=warn {extra-args} {files} | fix: buildifier -lint=fix {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| bundleAudit | bundle-audit | compatibility | verify: bundle-audit check {extra-args} {project-root} | — | workspace | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| byteOrderMarker | byte-order-marker | disabled | — | — | — | — | Disabled draft; no deferred support is claimed. |
| cargoCheck | cargo-check | compatibility | verify: cargo check --manifest-path {workspace-indicator} --quiet {extra-args} | — | workspace | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| cargoClippy | cargo-clippy | compatibility | verify: python -I -c <inline-script> {tool-executable} cargo-clippy verify {extra-args} __VELVET_GLOVE_CARGO_CLIPPY_WORKSPACE__ {workspace-indicator} | fix: python -I -c <inline-script> {tool-executable} cargo-clippy fix {extra-args} __VELVET_GLOVE_CARGO_CLIPPY_WORKSPACE__ {workspace-indicator} | fix: workspace | workspace | The isolated adapter rejects every extra argument, binds Cargo, rustc, rustdoc, cargo-clippy, and clippy-driver to one dedicated Rust 1.97.1 toolchain closure, ignores workspace and ambient Cargo configuration by running frozen offline commands from a private directory, and clears compiler, wrapper, loader, and Clippy overrides. Read-only metadata, a lint-capped coverage pass, and the authoritative Cargo JSON pass must prove one locked package and terminal completion; only fresh dep-info tied to selected-package artifacts may prove compilation of every physical workspace Rust source. The remedy applies only validated, non-overlapping MachineApplicable byte replacements transactionally; the final read-only phase remains authoritative, and workspace attribution is conservative while changed-file evidence is exact. This intentionally narrow contract rejects multi-package workspaces, workspace-local path-dependency sources, custom build targets, dormant or target-gated physical `.rs` files, and projects whose all-targets/all-features build is not jointly valid. Dependency and procedural-macro code still executes under the network sandbox, and file/config replacement races or a failed rollback cannot be eliminated. |
| cargoFmt | cargo-fmt | compatibility | verify: python -I -c <inline-script> {tool-executable} cargo-fmt rustfmt verify {extra-args} __VELVET_GLOVE_CARGO_FMT_WORKSPACE__ {workspace-indicator} | format: python -I -c <inline-script> {tool-executable} cargo-fmt rustfmt format {extra-args} __VELVET_GLOVE_CARGO_FMT_WORKSPACE__ {workspace-indicator} | format: workspace | workspace | The isolated adapter rejects every extra argument, roots each invocation at one unique Cargo.lock and its sibling Cargo.toml, binds Cargo, cargo-fmt, rustfmt, and rustc to the dedicated Rust 1.97.1/rustfmt 1.9.0 closure, runs locked offline children from a private directory, and clears Cargo, Rust, wrapper, cache, and loader overrides. Before touching the selected workspace it copies every validated file plus retained directory topology and modes, adds a parseable formatting defect to every physical `.rs` file, and requires `cargo fmt --all --check` to report exactly that complete set; this makes dormant or Cargo-excluded Rust sources fail closed. Status 1 counts as formatting issues only with a complete files-with-diff report and empty stderr. Formatting must change exactly its reported physical Rust files without altering retained directory topology or modes; an operational failure after the real child starts attempts to restore baseline file bytes, modes, and mtimes plus retained directory topology and modes while reporting rollback failure. File or directory inode identity and directory mtimes are not restored. Every normally completed child is checked for and sweeps remaining members of its owned process group, but a child that deliberately escapes into another session or process group cannot be contained by this adapter. The authoritative check plus repeated remedy proves the retained post-state and idempotence. Multi-member workspaces are supported, but external path dependencies, non-member manifests, nested rustfmt configs, symlinks, hard links, missing locks, and files beneath `.git`, `.velvet-glove`, `node_modules`, or `target` are outside the contract. Rustfmt proves formatting rather than compilation, and replacement races or an unsuccessful rollback cannot be eliminated. |
| checkAddedLargeFiles | check-added-large-files | disabled | — | — | — | — | Disabled draft; no deferred support is claimed. |
| checkCaseConflict | check-case-conflict | disabled | — | — | — | — | Disabled draft; no deferred support is claimed. |
| checkConventionalCommit | check-conventional-commit | disabled | — | — | — | — | Disabled draft; no deferred support is claimed. |
| checkExecutablesHaveShebangs | check-executables-have-shebangs | disabled | — | — | — | — | Disabled draft; no deferred support is claimed. |
| checkMergeConflict | check-merge-conflict | compatibility | verify: grep -lE ^(<{7} \\|\\\|{7} \\|={7}$\\|>{7} ) {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| checkSymlinks | check-symlinks | disabled | — | — | — | — | Disabled draft; no deferred support is claimed. |
| clangFormat | clang-format | compatibility | verify: clang-format --dry-run -Werror {extra-args} {files} | format: clang-format -i {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| cmakeFormat | cmake-format | compatibility | verify: cmake-format --check {extra-args} {files} | format: cmake-format --in-place {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| cocogittoCommitMsg | cocogitto-commit-msg | disabled | — | — | — | — | Disabled draft; no deferred support is claimed. |
| contextlint | contextlint | compatibility | verify: python -I -c <inline-script> node {tool-executable} {workspace-indicator} {extra-args} __VELVET_GLOVE_CONTEXTLINT_FILES__ {files} | — | workspace | workspace | The isolated adapter rejects extra arguments and empty or unknown-rule configs, binds absolute Node and CLI-JS paths to the exact @contextlint/cli and @contextlint/core 1.1.1 package pair, and runs a byte-stable private SEC-001 completion probe before accepting project output. The validated source config is copied to a mode-0600 private file and that authoritative copy is used for the project child. Config include patterns never control coverage: every physical Markdown file outside physical .git, node_modules, and .velvet-glove directories is bounded, snapshotted, and passed explicitly; mixed-case runner globs select every supported suffix, matching root and nested exclusions prevent skipped candidates, and every candidate must belong to the physical inventory. Every encountered symlink, hard-linked Markdown, a Contextlint-glob-magic component in an inventoried path, an empty inventory, more than 4,096 Markdown files, and more than 64 MiB of Markdown are rejected. A minimal child environment and Node permission mode allow reads only from the pinned package graph, lexical workspace root, and private config/probe root and grant no writes, child processes, workers, or native addons. Exact JSON validation maps warnings as well as errors to source issues, while incomplete, malformed, permission-denied, signal, spawn, mutation, normally exiting same-group descendants, and over-16-MiB results fail operationally. Physical skipped subtrees and non-Markdown objects are outside inventory; nested symlinks inside real skipped trees are unwalked but built-in existence checks may follow them through the lexical workspace grant. Concurrent workspace topology, referenced-target, Markdown, config, or executable replacement cannot be eliminated, and a deliberately escaped session or process group is outside containment. Node permissions are not an OS sandbox, so the active deny-network wrapper remains authoritative for network isolation. |
| cppLint | cpplint | compatibility | verify: cpplint {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| dclint | dclint | explicit | fix: python -I -c <inline-script> {tool-executable} verify {project-root} {extra-args} __VELVET_GLOVE_DCLINT_FILES__ {files} | fix: python -I -c <inline-script> {tool-executable} fix {project-root} {extra-args} __VELVET_GLOVE_DCLINT_FILES__ {files} | fix: target-files | fix: batch | The isolated adapter accepts only one validated JSON config override, binds dclint and Node through absolute managed paths, clears ambient Node, loader, debug, and dclint configuration channels, and validates the exact dclint 3.1.0 JSON result shape and rule-fixability table. The source config is normalized into a mode-0600 private copy: executable-loading, diagnostic-suppression, selection-changing, unknown-rule, unsafe-option, duplicate service-group membership, and incomplete top-level-order configurations are rejected before the native child; every enabled top-level-order write receives a complete Compose order that preserves extension properties and models. The unsafe no-version-field rule is marked nonfixable and always disabled, and an explicit enable is rejected because its native fixer can delete nested extension data even when a line-level disable suppresses the diagnostic. The adapter snapshots the retained project outside fixed skipped directories, preflights the complete selected batch, narrows the native write to files proven fixable, rejects topology, metadata, identity, and out-of-subset changes, and requires a full authoritative recheck; operational failures attempt to restore retained file bytes, modes, and mtimes plus directory topology and modes. TMPDIR must be an existing absolute directory outside the project and is canonicalized before private allocation. Child output, workspace size, file count, and cleanup are bounded, cancellation is forwarded to a dedicated process group, and normally exiting same-group descendants are swept. Findings remain conservatively attributed to every selected candidate; fixed skipped subtrees are outside the retained snapshot, file inode identity and directory mtimes are not restored, and concurrent replacement races, an unsuccessful rollback, and a deliberately escaped session or process group cannot be eliminated. |
| deadnix | deadnix | compatibility | verify: deadnix --fail {extra-args} {files} | fix: deadnix --edit {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| deno | deno | compatibility | verify: deno fmt --check {extra-args} {files} | format: deno fmt {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| denoCheck | deno-check | compatibility | verify: deno check {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| detectPrivateKey | detect-private-key | compatibility | verify: grep -lE -- -----BEGIN ([A-Z]+ )?PRIVATE KEY-----\\|PuTTY-User-Key-File- {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| dprint | dprint | compatibility | verify: dprint check --allow-no-files {extra-args} {files} | format: dprint fmt --allow-no-files {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| editorconfigChecker | editorconfig-checker | compatibility | verify: editorconfig-checker -no-color {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| erb | erb | compatibility | verify: erb_lint {extra-args} {files} | fix: erb_lint --autocorrect {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| errCheck | errcheck | compatibility | verify: python -I -c <inline-script> {tool-executable} go {extra-args} __VELVET_GLOVE_ERRCHECK_WORKSPACE__ {workspace-indicator} | — | workspace | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| eslint | eslint | compatibility | verify: python -I -c <inline-script> node {tool-executable} verify {extra-args} __VELVET_GLOVE_ESLINT_FILES__ {files} | fix: python -I -c <inline-script> node {tool-executable} fix {extra-args} __VELVET_GLOVE_ESLINT_FILES__ {files} | fix: target-files | batch | The isolated adapter binds absolute Node and ESLint CLI paths to the exact Node 24.19.0, npm 11.17.0, and ESLint 10.8.1 closure, rejects every extra argument, and never discovers project ESLint config, plugins, parsers, processors, ignore files, inline configuration, or suppressions. Only unique UTF-8 .js, .cjs, and .mjs files are accepted; TypeScript and JSX are deliberately outside this core contract. An optional strict .velvet-glove-eslint.json document can set only built-in rule severities from a fixed allowlist and is translated to mode-0600 private CJS configuration alongside private suppressions and per-child cache state. A minimal child environment clears ambient Node, npm, ESLint, loader, home, and cache channels. Exact bounded JSON completion evidence must cover every selected file, and verify must leave source bytes and identities unchanged. Fix first performs a complete read-only check and native --fix-dry-run, writes only files whose exact output was predicted, checks every resulting byte and identity, and then verifies the complete batch; repeated remedy proves idempotence. Warnings and errors remain conservatively attributed to every selected candidate, while exact changed-file evidence records mutations. Symlinked, hard-linked, oversized, non-UTF-8, and aliased selections are unsupported. A selected path can still be replaced or changed after validation, a late native write failure can leave partial writes, and a deliberately escaped session or process group is outside containment; the adapter intentionally does not attempt an unsafe rollback. |
| fasterer | fasterer | compatibility | verify: fasterer {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| fixSmartQuotes | fix-smart-quotes | disabled | — | — | — | — | Disabled draft; no deferred support is claimed. |
| flake8 | flake8 | compatibility | verify: flake8 {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| ghalintAction | ghalint-action | compatibility | verify: ghalint run-action {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| ghalintWorkflow | ghalint-workflow | explicit | verify: python -I -c <inline-script> {tool-executable} {project-root} {extra-args} __VELVET_GLOVE_GHALINT_WORKFLOW_FILES__ {files} | — | verify: workspace | verify: workspace | The isolated adapter is pinned to the ghalint 1.5.6 source diagnostic grammar and accepts only an optional normalized relative YAML config override. It inventories exactly the native top-level `.github/workflows/*.yml`-then-`*.yaml` set, rejects zero or out-of-inventory selected candidates, and snapshots the bounded retained project outside fixed skipped directories before and after execution. A mode-0600 private config copy, absolute executable identity, minimal child environment, exact version probe, bounded output, signal forwarding, and same-process-group cleanup constrain the native child. Status 1 maps to source issues only when every timestamped log record matches the closed policy, workflow-parse, or configuration-error grammar, including policy-specific `action` and `env_name` fields and the pinned structured parse/config references; any other status, output, mutation, or record maps operationally. Every physical retained file, directory, workflow, and config must remain identical, while findings from the authoritative full native workspace scan are conservatively attributed across the selected candidate subset. Symlinks and hard-linked files in the retained project are unsupported, skipped subtrees are outside the snapshot, and concurrent replacement races or a deliberately escaped session or process group cannot be eliminated. The pinned source build carries a checksum-locked x/text closure update because the upstream release binary embeds an older Go toolchain. |
| gitleaks | gitleaks | compatibility | verify: gitleaks dir --redact --verbose --no-color --no-banner {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| goFmt | go-fmt | explicit | format: python -I -c <inline-script> {tool-executable} verify {extra-args} __VELVET_GLOVE_GOFMT_FILES__ {files} | format: python -I -c <inline-script> {tool-executable} write {extra-args} __VELVET_GLOVE_GOFMT_FILES__ {files} | format: target-files | format: batch | The isolated adapter rejects extra arguments, resolves the managed gofmt executable before fixing the child environment, and accepts only normalized absolute selected paths that are unique regular files with one link. Every verify runs `gofmt -l`; because native dirty output still exits zero, only complete ordered selected-path lines are accepted as source issues, while native status 2 dominates any partial stdout and remains operational failure. Every write is preceded by the same read-only list preflight, so a parse or read failure detected there cannot partially format an otherwise valid batch; successful writes must preserve file identities, and deferred writes are followed by the authoritative workflow check. Go, loader, and debug overrides are scrubbed, output and selected bytes are bounded, and cancellation forwards to a dedicated child process group with bounded cleanup. Symlinked and hard-linked selections are deliberately unsupported. Preflight/file snapshots cannot eliminate concurrent path-replacement races, and native multi-file `-w` is nontransactional, so a late write-time I/O failure may leave earlier files mutated. These semantics are pinned to Go 1.26.5. |
| goFumpt | gofumpt | explicit | format: gofumpt -l {extra-args} {files} | format: gofumpt -w {extra-args} {files} | format: target-files | format: batch | Read-only list mode reports dirty files through stdout with exit 0; parse failures depend on the installed tool version. |
| goImports | goimports | explicit | format: goimports -l {extra-args} {files} | format: goimports -w {extra-args} {files} | format: target-files | format: batch | Read-only list mode reports dirty files through stdout with exit 0; parse failures depend on the installed tool version. |
| goLines | golines | explicit | format: golines --dry-run {extra-args} {files} | format: golines -w {extra-args} {files} | format: target-files | format: batch | Read-only dry-run diffs are detected through stdout; the original upstream is archived and installed-version behavior may vary. |
| goSec | gosec | compatibility | verify: gosec -quiet {extra-args} ./... | — | workspace | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| goVet | go-vet | compatibility | verify: python -I -c <inline-script> {tool-executable} {extra-args} __VELVET_GLOVE_GO_VET_WORKSPACE__ {workspace-indicator} | — | workspace | batch | The isolated adapter rejects every extra argument and ambient Go override, requires exact Go 1.26.5, fixes read-only offline module semantics with CGO disabled, and gives each invocation private home, temporary, Go-path, and fresh build-cache roots. Exact `go env`, `go mod verify`, and concatenated `go list -json` preflights bind the main module, physical Go inventory, package scope, and internal/external test actions before `go vet -json -mod=readonly ./...` runs. Native vet findings exit zero, so only a complete UTF-8 JSON-object stream whose package/action count, analyzer results, canonical paths, byte positions, related information, and suggested fixes validate maps to status 1; native nonzero, stderr, embedded errors, malformed or incomplete scope, mutation, and over-limit output map to status 2. Empty `{}` action objects are conservatively accepted only when their total count equals the trusted package/test action count; Go does not identify those objects, so they cannot prove which clean action each represents. Physical build-tag-ignored files are accepted only when `go list` reports them ignored, cgo-only scopes are unsupported, and local replacements or already-cached external modules remain read dependencies outside the workspace snapshot. Replacement races restored between snapshots and descendants that deliberately escape their owned process group cannot be eliminated. The active deny-network wrapper remains authoritative. |
| goVulnCheck | govulncheck | compatibility | verify: govulncheck {extra-args} ./... | — | workspace | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| golangciLint | golangci-lint | compatibility | verify: golangci-lint run {extra-args} ./... | fix: golangci-lint run --fix {extra-args} ./... | fix: workspace | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| gomodTidy | gomod-tidy | explicit | tidy: go mod tidy -diff {extra-args} | tidy: go mod tidy {extra-args} | tidy: workspace | tidy: workspace | Requires a Go release with `go mod tidy -diff`; exit 1 is treated as source issues and may be ambiguous with some command failures. |
| googleJavaFormat | google-java-format | compatibility | verify: google-java-format --dry-run --set-exit-if-changed {extra-args} {files} | format: google-java-format --replace {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| hadolint | hadolint | compatibility | verify: hadolint {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| harper | harper | compatibility | verify: harper-cli --no-color lint --format compact {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| harperCommitMessage | harper-commit-message | compatibility | verify: harper-cli --no-color lint --format compact {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| hclfmt | hclfmt | compatibility | verify: terragrunt hcl fmt --check {extra-args} | format: terragrunt hcl fmt {extra-args} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| isort | isort | compatibility | verify: isort --check-only {extra-args} {files} | fix: isort {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| jq | jq | compatibility | verify: jq empty {extra-args} {files} | — | target-files | per-file | The per-file parse check accepts an empty stream and multiple whitespace-separated top-level JSON values; exact-one-document validation is not claimed. |
| justFormat | just-format | compatibility | verify: just --fmt --unstable --check {extra-args} --justfile {files} | format: just --fmt --unstable {extra-args} --justfile {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| knip | knip | compatibility | verify: knip --no-progress --directory {workspace} {extra-args} | fix: knip --fix --no-progress --directory {workspace} {extra-args} | fix: workspace | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| knipStrict | knip-strict | compatibility | verify: knip --strict --no-progress --directory {workspace} {extra-args} | fix: knip --fix --strict --no-progress --directory {workspace} {extra-args} | fix: workspace | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| ktlint | ktlint | compatibility | verify: ktlint {extra-args} {files} | fix: ktlint -F {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| luacheck | luacheck | compatibility | verify: luacheck {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| lychee | lychee | compatibility | verify: lychee --no-progress {extra-args} -- {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| markdownLint | markdownlint | compatibility | verify: markdownlint {extra-args} {files} | fix: markdownlint --fix {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| mdschema | mdschema | compatibility | verify: mdschema check {extra-args} {files} | — | workspace | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| mise | mise | compatibility | verify: mise fmt --check {extra-args} | format: mise fmt {extra-args} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| mixCompile | mix-compile | compatibility | verify: mix compile --warnings-as-errors {extra-args} | — | workspace | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| mixFmt | mix-fmt | compatibility | verify: mix format --check-formatted {extra-args} {files} | format: mix format {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| mixTest | mix-test | compatibility | verify: mix test {extra-args} | — | workspace | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| mixedLineEnding | mixed-line-ending | disabled | — | — | — | — | Disabled draft; no deferred support is claimed. |
| mypy | mypy | compatibility | verify: mypy {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| newlines | newlines | disabled | — | — | — | — | Disabled draft; no deferred support is claimed. |
| nil | nil | compatibility | verify: nil diagnostics --deny-warnings {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| nixFmt | nixfmt | compatibility | verify: nixfmt --check {extra-args} {files} | format: nixfmt {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| nixfDiagnose | nixf-diagnose | compatibility | verify: nixf-diagnose {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| nixpkgsFormat | nixpkgs-fmt | compatibility | verify: nixpkgs-fmt --check {extra-args} {files} | format: nixpkgs-fmt {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| noCommitToBranch | no-commit-to-branch | disabled | — | — | — | — | Disabled draft; no deferred support is claimed. |
| oxfmt | oxfmt | compatibility | verify: oxfmt --check {extra-args} {files} | format: oxfmt --write {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| oxlint | oxlint | compatibility | verify: oxlint --deny-warnings {extra-args} {files} | fix: oxlint --deny-warnings --fix {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| phpCs | php-cs | compatibility | verify: phpcs {extra-args} {files} | fix: phpcbf {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| pinact | pinact | compatibility | verify: pinact run --verify --check {extra-args} {files} | fix: pinact run --verify {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| pinactUpdate | pinact-update | compatibility | verify: pinact run --update --verify --check {extra-args} {files} | fix: pinact run --update --verify {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| pkl | pkl | compatibility | verify: pkl eval {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| pklFormat | pkl-format | compatibility | verify: pkl format --diff-name-only {extra-args} {files} | format: pkl format --write {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| prettier | prettier | compatibility | verify: python -I -c <inline-script> node {tool-executable} verify {extra-args} __VELVET_GLOVE_PRETTIER_FILES__ {files} | format: python -I -c <inline-script> node {tool-executable} format {extra-args} __VELVET_GLOVE_PRETTIER_FILES__ {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| pylint | pylint | compatibility | verify: pylint --score=no {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| pythonCheckAst | python-check-ast | compatibility | verify: python3 -m py_compile {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| pythonDebugStatements | python-debug-statements | compatibility | verify: grep -nH -E ^[^#]*(\bbreakpoint\(\)\\|pdb\.set_trace\(\)) {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| reek | reek | compatibility | verify: reek {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| revive | revive | compatibility | verify: revive -set_exit_status {extra-args} ./... | — | workspace | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| rubocop | rubocop | compatibility | verify: rubocop --force-exclusion {extra-args} {files} | fix: rubocop --force-exclusion --autocorrect {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| ruff | ruff | explicit | lint: ruff check --force-exclude {extra-args} {files}; format: ruff format --quiet --force-exclude --check {extra-args} {files} | lint: ruff check --force-exclude --fix {extra-args} {files}; format: ruff format --quiet --force-exclude {extra-args} {files} | lint: target-files; format: target-files | lint: batch; format: batch | Lint remedies precede format remedies when both are initially dirty; a lint fix that dirties an initially clean format check is reported for manual follow-up after the bounded pass. |
| ruffFormat | ruff-format | compatibility | verify: ruff format --quiet --force-exclude --check {extra-args} {files} | format: ruff format --quiet --force-exclude {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| rumdl | rumdl | compatibility | verify: rumdl check --quiet --color never {extra-args} {files} | fix: rumdl check --quiet --color never --fix {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| rustfmt | rustfmt | compatibility | verify: rustfmt --check {extra-args} {files} | format: rustfmt {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| ryl | ryl | compatibility | verify: ryl {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| selene | selene | compatibility | verify: selene {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| shellcheck | shellcheck | compatibility | verify: shellcheck {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| shfmt | shfmt | compatibility | verify: shfmt -d --apply-ignore {extra-args} {files} | format: shfmt -w --apply-ignore {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| sorbet | sorbet | compatibility | verify: srb tc {extra-args} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| sortPackageJson | sort-package-json | compatibility | verify: sort-package-json --check {extra-args} {files} | format: sort-package-json {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| sqlFluff | sql-fluff | compatibility | verify: sqlfluff lint {extra-args} {files} | fix: sqlfluff fix {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| standardJs | standard-js | compatibility | verify: standard {extra-args} {files} | fix: standard --fix {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| standardRb | standard-rb | compatibility | verify: standardrb {extra-args} {files} | fix: standardrb --fix {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| staticcheck | staticcheck | compatibility | verify: staticcheck {extra-args} ./... | — | workspace | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| stylelint | stylelint | compatibility | verify: stylelint {extra-args} {files} | fix: stylelint --fix {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| stylua | stylua | compatibility | verify: stylua --check {extra-args} {files} | format: stylua {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| swiftlint | swiftlint | compatibility | verify: swiftlint lint {extra-args} {files} | fix: swiftlint --fix {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| taplo | taplo | compatibility | verify: taplo lint {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| taploFormat | taplo-format | compatibility | verify: taplo format --check {extra-args} {files} | format: taplo format {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| terraform | terraform | compatibility | verify: terraform fmt -check {extra-args} {files} | format: terraform fmt {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| tfLint | tflint | compatibility | verify: tflint {extra-args} | fix: tflint --fix {extra-args} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| tofu | tofu | compatibility | verify: tofu fmt -check {extra-args} {files} | format: tofu fmt {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| tombi | tombi | compatibility | verify: tombi lint --quiet {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| tombiFormat | tombi-format | compatibility | verify: tombi format --check {extra-args} {files} | format: tombi format {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| trailingWhitespace | trailing-whitespace | disabled | — | — | — | — | Disabled draft; no deferred support is claimed. |
| tsc | tsc | compatibility | verify: tsc --noEmit -p {workspace-indicator} {extra-args} | — | workspace | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| tsserver | tsserver | compatibility | verify: tsc-files --noEmit {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| ty | ty | compatibility | verify: ty check {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| typos | typos | compatibility | verify: typos --force-exclude --color never {extra-args} {files} | fix: typos --force-exclude --color never --write-changes {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| vacuum | vacuum | compatibility | verify: python -I -c <inline-script> {tool-executable} {extra-args} __VELVET_GLOVE_VACUUM_FILES__ {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| vale | vale | compatibility | verify: vale --output=line --no-wrap {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| vpCheck | vp-check | compatibility | verify: vp check {extra-args} {files} | fix: vp check --fix {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| vpFmt | vp-fmt | compatibility | verify: vp fmt --check {extra-args} {files} | format: vp fmt {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| vpLint | vp-lint | compatibility | verify: vp lint --deny-warnings {extra-args} {files} | fix: vp lint --deny-warnings --fix {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| xmllint | xmllint | compatibility | verify: xmllint --noout {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| xo | xo | compatibility | verify: xo {extra-args} {files} | fix: xo --fix {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| yamlfmt | yamlfmt | compatibility | verify: yamlfmt -lint {extra-args} {files} | format: yamlfmt {extra-args} {files} | format: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
| yamllint | yamllint | compatibility | verify: yamllint --strict {extra-args} {files} | — | target-files | batch | Read-only checks are compatibility-translated as batched invocations; real-tool behavior is version-dependent. |
| yq | yq | explicit | format: sh -c tool=$1; file=$2; shift 3; tmp=$(mktemp "${TMPDIR:-/tmp}/hookkit-yq.XXXXXX") \\|\\| exit 2; trap 'rm -f "$tmp"' 0 HUP INT TERM; "$tool" -P "$@" "$file" >"$tmp" \\|\\| exit 2; diff -u "$file" "$tmp" yq-check {tool-executable} {files} -- {extra-args} | format: yq -iP {extra-args} {files} | format: target-files | format: per-file | Per-file check requires POSIX `sh`, `mktemp`, and `diff`; formatting behavior depends on the installed yq version. |
| zizmor | zizmor | compatibility | verify: zizmor --no-progress -qq {extra-args} {files} | fix: zizmor --no-progress -qq --fix {extra-args} {files} | fix: target-files | batch | Each remedy is compatibility-paired with the final read-only phase; real-tool behavior is version-dependent. |
