# Third-Party Licenses

This file lists the licenses of third-party Rust crates that are linked into
binaries built from this repository.

It is generated by [`cargo about`](https://github.com/EmbarkStudios/cargo-about)
from `about.toml` and `about.hbs`. Do not edit by hand. Run
`scripts/regen-licenses.sh` to refresh after changing dependencies; CI rejects
pull requests where this file is out of date.

Velvet Glove and the Git-pinned `agent-hook-kit` framework crates are
dual-licensed under MIT and Apache-2.0; see `LICENSE-MIT` and
`LICENSE-APACHE`.

## Third-party crates

| Crate | Version | Declared license (SPDX) |
| --- | --- | --- |
{{#each crates}}
{{#if package.source}}
| `{{package.name}}` | {{package.version}} | `{{package.license}}` |
{{/if}}
{{/each}}

## License texts

The following sections reproduce the verbatim license file from each
third-party crate. Crates listed under the same heading ship identical license
text; minor textual variants (e.g. different copyright lines) are listed
separately.

{{#each licenses}}
{{#if used_by.0.crate.source}}
### {{name}} — text shipped by `{{used_by.0.crate.name}}`

Used by:

{{#each used_by}}
{{#if crate.source}}
- `{{crate.name}}` {{crate.version}}{{#if crate.repository}} — <{{crate.repository}}>{{/if}}
{{/if}}
{{/each}}

```
{{{text}}}
```

{{/if}}
{{/each}}
